using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class attach : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //GameObject.Find("quadArm1").transform.parent = gameObject.transform;
        ////gameObject.transform.parent = GameObject.Find("quadArm1").transform;
        //GameObject.Find("quadArm1").SetActive(false);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
